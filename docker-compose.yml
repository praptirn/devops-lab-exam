version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://praptinayak2005_db_user:kaBeeRaa@cluster1.aunpexn.mongodb.net/?appName=Cluster1

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
